import React from "react";

const Header: React.FC = (): React.ReactElement => {
  return (
    <div className="w-full bg-blue-400 h-16 flex items-center justify-center text-white">
      <h2 className="uppercase">Home - NewCombin Code Challenge</h2>
    </div>
  );
};

export default Header;
